name: 'Label Actions PR'

on:
  pull_request:
    types: [opened, reopened]

permissions:
  contents: read
  issues: write
  pull-requests: write
  discussions: write

jobs:
  action:
    if: ${{ github.event.pull_request.user.login != 'dependabot[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.1.0
        with:
          token: ${{ secrets.TOKEN }}
      - uses: actions/labeler@main  # v4.3.0
        with:
          dot: true
          repo-token: ${{ secrets.TOKEN }}
          configuration-path: ".github/actions_labeler.yaml"